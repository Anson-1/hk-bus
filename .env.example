# Copy this file to .env and set your own values before running docker-compose.
# The defaults shown here match the Kubernetes secrets in k8s/postgres/secret.yaml.

# PostgreSQL
POSTGRES_PASSWORD=postgres

# Grafana
GRAFANA_USER=admin
GRAFANA_PASSWORD=hkbus123
